#include<iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Rus");
	int a1,d, a100, S;
	cout << "Введите первый член арифметической прогрессии: ";
	cin >> a1;
	cout << "Введите сотый член арифметической прогрессии: ";
	cin >> a100;

	d = (a100 - a1) / 99;
	S = (2 * a1 + d * 20) * 21 / 2;
	cout << "Разность арифметической прогрессии: " << d << endl << "Сумма арифметической прогрессии: " << S;
	
}